# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv4 connections from Docker network
host    all             all             172.16.0.0/12           scram-sha-256

# IPv4 connections from local network (adjust as needed)
host    all             all             192.168.0.0/16          scram-sha-256

# # IPv4 connections from anywhere (development only - remove in production)
# host    all             all             0.0.0.0/0               scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# IPv6 connections from anywhere (development only - remove in production)
host    all             all             ::/0                    scram-sha-256

# Replication connections
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256
